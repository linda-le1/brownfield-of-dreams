<section class="invite-new">
  <h1>Invite a Friend!</h1>
    <%= form_tag '/invite', method: :post do |f|%>
      <%= label_tag :github_handle %>
      <%= text_field_tag :github_handle %>

      <%= submit_tag 'Send Invite', class: "btn btn-primary mb1 bg-teal" %>
    <% end %>
</section>
